// Archived: Removed Download Model Button as per user request — 2026-03-08
/*
if (!ready)
  Padding(
    padding:
        const EdgeInsets.symmetric(horizontal: 16, vertical: 8),
    child: SizedBox(
      width: double.infinity,
      child: OutlinedButton(
        onPressed: () =>
            GemmaModelService.startBackgroundDownload(),
        child: const Text('Download Model Now'),
      ),
    ),
  ),
*/
